---
name: unattended
description: 사용자가 자리를 비운 채 run을 돌리는 무인 모드. 토글이다 — "/unattended", "무인 모드", "무인모드"를 부르면 꺼져 있으면 켜고 켜져 있으면 끈다. "무인으로 돌려", "자리 비울 테니 알아서 진행해", "내일 아침까지 끝내 놔"는 켜기, "돌아왔어", "무인 해제", "/unattended off"는 끄기, "/unattended on"은 켜기로 고정. 켜져 있으면 리더는 "사용자에게 올린다"고 된 모든 지점을 위임장(mandate)의 대체 규칙으로 처리하고, 위임장 밖의 결정은 질문 큐에 적고 그 결정에 의존하지 않는 task만 계속 돌린다. 끄면 질문 큐부터 처리한다. 상태는 파일에 있어 컨텍스트가 압축돼도 유지된다. 배포와 외부 전송은 무인에서도 하지 않는다. git 작업(커밋·푸시·PR·머지)은 git-workflow대로 한다.
---

# unattended — 사용자 없이 도는 run

사용자가 답해야 넘어가는 지점(결정 결손, 승인, 결정 확정, 미검증, 범위 확장)마다 **대체 규칙**을 둔다.

1. **리더 혼자 결정하지 않는다.** 결정이 비어 있으면 조사·벤치마크로 근거를 만들고, 아키텍트가 대안을 비교해 추천하고, critic이 비평한 뒤 리더가 확정한다. 요구가 모호하면 비슷한 제품을 조사·벤치마크해 요구를 정의하고 PRD에 근거를 적는다. 질문 큐는 위임장 범위 밖인 것에만 쓴다. 사용자는 끝난 뒤 run 보고서로 무엇을 왜 정했는지 확인한다.
2. **되돌릴 수 있는 쪽으로.** 물어볼 사람이 없으니 갈림길에서는 되돌릴 수 있는 대안을 고르고 재검토 조건을 남긴다.
3. **질문은 큐에, run은 계속.** 막힌 결정에 의존하지 않는 task는 돌린다. 의존하는 것은 리더가 `ready`로 올리지 않고 `pending`에 둔다.

## 토글

상태는 파일에 있다. 대화 기억에 두지 않는다.

```
호출됨 ("/unattended", "무인 모드", "무인모드")
  상태 파일 없음 또는 off → 켠다
  상태 파일 on            → 끈다
인수 on / 켜기 말("무인으로 돌려", "알아서 진행해") → 켠다 (이미 켜져 있으면 그대로, 위임장만 갱신)
인수 off / 끄기 말("돌아왔어", "무인 해제")      → 끈다 (이미 꺼져 있으면 그대로)
```

인수로 위임장 항목을 줄 수 있다 (`/unattended 범위: 백엔드만, 미검증: 진행`). 켤 때만 의미가 있다.

**켤 때**

1. 위임장을 읽는다. 없으면 **위임장 템플릿으로 사용자와 함께 채운다.** 채우기 전에 켜지 않는다.
2. 상태 파일에 `on`, 시각, 위임장 버전을 쓴다. notes에도 한 줄.
3. 이후 원장 log의 `--because`에는 적용한 위임장 조항(`mandate §n`)을 함께 적는다.
4. 진행 중인 run이 있으면 그 run의 열린 질문부터 위임장으로 닫아 본다.
5. 사용자에게 한 줄로 답한다: "무인 모드 켬. 위임장 v<n>. 끄려면 다시 부르거나 '돌아왔어'."

**끌 때**

1. 상태 파일에 `off`와 시각. notes에 한 줄.
2. 질문 큐를 순서대로 올린다. 답을 받아 막힌 task를 투입한다.
3. 자동 결정 목록을 보인다. 사용자가 뒤집는 것은 `superseded` + 의존 task 재작업.
4. 한 줄로 답한다: "무인 모드 끔. 질문 <n>건, 자동 결정 <m>건 — 위부터."

**매 판단 전에** 리더는 상태 파일을 읽는다. 켜져 있으면 대체 규칙, 꺼져 있으면 유인 절차 그대로. 기억으로 판단하지 않는다.

## 위임장

문서 템플릿 `mandate.md`로 사용자와 함께 채운다.

사용자가 "다 알아서"라고 하면 그것도 위임장이다 — 범위 상한만 요구 문장으로 두고, 되돌리기 어려운 결정도 조사·아키텍트·critic을 거쳐 리더가 확정한다. 질문 큐에는 범위 밖만 남긴다.

## 대체 규칙

"사용자에게" 올리게 돼 있는 지점마다.

| 지점 | 유인 | 무인 |
|---|---|---|
| 결정 결손 (intake) | 묻는다 | 위임장 기본값 → 없으면 아키텍트에게 보내 조사·벤치마크로 정한다. 요구가 모호하면 비슷한 제품 조사로 요구를 정의한다. 범위 밖만 질문 큐 |
| 아키텍트의 NFR 질문 | 묻는다 | 기본값 → 없으면 조사 task로 근거(비슷한 규모의 실측, 공식 문서)를 찾아 아키텍트가 값을 정하고 근거를 적는다. 근거를 찾을 수 없으면 보수적 값으로 정하고 이유를 적는다. 자동 결정 목록에 올린다 |
| task 표 승인 | 사용자 | critic blocker 0 + 모든 결정이 위임장 안 → 자동 승인, notes에 기록. 하나라도 밖 → 그 항목만 큐, 의존 안 하는 task는 승인 |
| 되돌리기 어려운 결정 확정 | 사용자가 고름 | 허용된 종류면 아키텍트 추천 + critic 비평 무이의 + doc-reviewer confirmed → 확정한 사람을 "자동 (위임장 §n)"으로 적는다. 아니면 `deferred` 가능한지 먼저 보고, 안 되면 큐 |
| 비판 항목 중 근거 부족 | 사용자 | 조사 task를 추가해 근거를 만들고 round를 다시 돈다 |
| 입력 없음으로 task 추가 | 한 줄 확인 | 범위 상한 안이면 추가 + 기록. 밖이면 큐 |
| 범위 확장 | 승인 | 같음 |
| 재작업 2회 초과 | 리더 | 리더가 원인을 찾는다. 상위 문서가 비었으면 그 문서를 고치고(critic 비평 → 수정) 재작업을 다시 시작한다. 큐에 올리지 않는다 |
| 미검증 (`inconclusive`) | 리더 | 환경을 만드는 task를 추가해 검증한다. 만들 수 없는 환경(실키, 외부 계정)이면 미검증으로 표시하고 run 보고서에 적는다 |
| 요구 오독 (비판에서 요구 해석 blocker) | 사용자 | 요구 원문과 비슷한 제품 조사로 해석을 다시 세우고 PRD를 고친다. 그 PRD를 critic이 다시 비평한다 |
| 커밋·푸시·PR·머지 | git-workflow대로 | 같음 |
| 배포, 외부 전송 | 사용자 | **항상 멈춘다.** 준비까지 하고 끝낸다 |
| 상한 도달 | — | 진행 중 task는 끝내고 새로 던지지 않는다. 종료 보고 |

자동 결정에는 항상 두 인스턴스가 관여한다 — 추천한 쪽과 검증한 쪽. 리더 혼자 닫는 결정은 무인에서도 없다.

## 질문 큐

질문 큐는 run 보고서의 §질문 절에 적는다. 형식은 run 보고서 템플릿을 따른다.

취한 가정이 있으면 그 가정으로 만든 것에 `[가정 Q2]`를 남긴다.

## 종료 보고

켜진 채 run이 끝나면 종료 보고는 결과보다 **질문 큐와 자동 결정 목록이 먼저**다. run 보고서에 §자동 결정 (결정, 근거 조항, 추천한 쪽, 검증한 쪽) 절을 둔다. run이 끝나도 모드는 저절로 꺼지지 않는다 — 사용자가 끈다.

사용자의 메시지가 오면 리더는 먼저 "돌아온 것인가"를 본다. 끄기 말이면 끈다. 그냥 다음 요구면 켜진 채 진행하되 "무인 모드가 켜져 있다"를 한 줄 알린다.

## 흔한 실패

| 증상 | 원인 |
|---|---|
| 아침에 보니 첫 결손에서 멈춰 있음 | 위임장 없이 켬 |
| 압축 뒤 리더가 사용자에게 묻고 멈춤 | 상태를 기억에 둠. 파일을 읽을 것 |
| 켠 줄 알았는데 꺼짐 | 토글을 두 번 부름. 확실히 하려면 `on`/`off`를 붙일 것 |
| 리더가 스택을 정해 버림 | 대체 규칙 위반. 추천+검증 없는 단독 결정 |
| 질문 큐가 비어 있는데 가정이 잔뜩 | 큐에 안 적고 notes에만 적음. 사용자가 못 찾는다 |
| 무인으로 배포됨 | "항상 하지 않는 것" 위반. 위임장으로도 열 수 없는 항목 |
| 되돌리기 어려운 결정을 "deferred"로 넘김 | 미루기 기준 오용. 바꾸면 다시 만들어야 하면 큐다 |
