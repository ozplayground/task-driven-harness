# task-driven-harness

이 저장소는 하네스의 소스다. run 규칙(`orchestration`, `git-workflow`, 문서 표준 등)은 여기에 적용하지 않는다. 파일을 직접 고치고 커밋한다. CHANGELOG는 릴리스 버전이 바뀔 때만 적는다.

사용자가 설치할 경로를 말하면 `./install.sh <경로>`를 돌리고, 결과를 확인해 보고한다. 설치된 프로젝트에서는 새 세션을 열어야 하네스가 로드된다는 것을 함께 알린다.
